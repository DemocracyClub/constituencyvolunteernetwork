# Create your views here.

def task(request, task_slug, login_token=""):
    """
        Check out a task. Needs to also optionally take a login token to allow
        direct access to the task by email.
    """
    pass